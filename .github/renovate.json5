{
  "regexManagers": [
      {
        "datasourceTemplate": "github-releases",
        "fileMatch": [
          "argocd-config/base/.*\\.yaml"
        ],
        "matchStrings": [
          " +targetRevision: +(?<currentValue>[^'\" ]+) renovate: depName=argoproj/argo-helm extractVersion=^argo-cd-(?<version>.+)$
        ],
      },
    ]
}
